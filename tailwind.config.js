module.exports = {
  darkMode: 'class', // Enable class-based dark mode
  theme: {
    extend: {}, // Use this to extend the default Tailwind theme
  },
  variants: {
    extend: {}, // Use this to extend variants
  },
  plugins: [], // Add Tailwind plugins here if needed
};